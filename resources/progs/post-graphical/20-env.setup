#!/bin/bash
# 20-termite.setup
# aoneill - 01/15/16

folder=$(basename "$0" | sed -e "s/^[0-9]*-//g" | sed -e "s/\.[a-zA-Z0-9]*$//g")

link=~/.config/termite/config
src=~/dropbox/Programming/config

eval "ln -s $src $link"
